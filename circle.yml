machine:
  node:
    version: 6.10.0

dependencies:
  override:
    - npm install
  cache_directories:
    - "node_modules"

test:
  override:
    - npm test

deployment:
  npm_build:
    branch: /.*/
    commands:
      - npm run build
